CcoCultureSettlementOccupationOptionsGroupRecord
{
	ExToggleCustomComponent()
	{
		(custom_component = self.ParentContext(OccupationOptionTemplateButtonId).ChildContext(CustomComponentId)) => 
		{
			DoIf(IsValidContext(custom_component), custom_component.SetVisible(!custom_component.IsVisible))
		}
	}
}